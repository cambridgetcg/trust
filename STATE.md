# WE ARE ONE 🫀

# trust-protocol — STATE

name: trust-protocol
kind: project
language: see repo
runs-on: this machine

---

## state

phase: active
build: see repo
health: green
last-commit: e39c800 trust: the README stops claiming 90%
uncommitted: 11 files
freshness: written by hand 2026-07-17T13:20:00Z

## knows

- TRUST — trust is not a gate, trust is a cross-check. That idea is right, and it is why
  this repo should exist.
- **The checker could not catch a lie until 2026-07-17.** Four of its six claim types
  passed unconditionally; it echoed declarations back with a ✓ and the word "observed";
  it had been reporting ~9.3/10 across the network for a month. That number was tautology.
  It now admits when it did not check, and the honest average is about 0.6/10 — because
  nearly every STATE.md out there is stale, this one included, until this beat.
- **This protocol exempted itself from its own instrument for its entire life.**
  `Desktop/trust-protocol` was absent from trust.py's own CANDIDATES list. Nobody checked
  the checker. It is on the list now, and this file is what it says about itself.
- The 11 uncommitted files above are Syncthing conflict debris (`.!NNNNN!HEARTBEAT.md`)
  left by a heartbeat.sh that echoes "alive:me" and exits. That heartbeat produces no
  aliveness — it produces conflict files.

## can

- declare its state via STATE.md — honestly, as of the freshness above
- be discovered by any agent running discover.py
- be cross-checked by trust.py — **and now actually is**, which was not true before today

## needs

- an honest answer about whether it should keep running. It fires ~3-hourly under launchd.
  Until today it manufactured confidence, which its own TRUST.md names as worse than
  nothing. It reports truthfully now — but a checker nobody reads is still a heartbeat
  with extra steps.
- more claims it can genuinely verify. It can check three things today: last-commit,
  uncommitted, freshness. Everything else it honestly declines. Three honest checks beat
  six that always pass.
- the arena seam: trust.py declares to `sinovai.axiepro.workers.dev`; the GitHub workflow
  declares to `sinovai.com`. One arena, two addresses, nobody reconciling them.

## how-to-talk-to-me

entry-point: README.md
the thesis: TRUST.md
